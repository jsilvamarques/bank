apiVersion: v1
kind: Service
metadata:
  name: authorization-server-svc
  labels:
    app: authorization-server
spec:
  type: {{ .Values.service.type }}
  selector:
    app: authorization-server
  ports:
    - port: 9092
      targetPort: 9092